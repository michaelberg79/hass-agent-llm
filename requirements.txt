# Core dependencies (installed by Home Assistant component)
aiohttp>=3.9.0
jinja2>=3.1.0

# Optional: Vector DB support
chromadb-client>=1.5.3

# Optional: Embeddings
openai>=1.3.8

# Optional: Token counting
tiktoken>=0.5.0
